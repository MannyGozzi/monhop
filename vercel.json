{
  "installCommand": "npm --prefix site ci --no-fund --no-audit",
  "buildCommand": "npm --prefix site run build",
  "outputDirectory": "site/dist",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
